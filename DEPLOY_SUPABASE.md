# Supabase 数据库配置指南

本文档详细说明如何使用 Supabase 作为 Grok2API 的外部数据库。

## 🌟 为什么选择 Supabase？

Supabase 是一个开源的 Firebase 替代品，提供免费的 PostgreSQL 数据库。

**优势：**
- ✅ **完全免费**：500 MB 存储 + 无限 API 请求
- ✅ **亚洲节点**：访问速度快（新加坡节点）
- ✅ **易于使用**：Web 界面管理
- ✅ **自动备份**：每日自动备份
- ✅ **高可用性**：99.9% 可用性保证
- ✅ **无需信用卡**：注册即可使用

**与 Railway PostgreSQL 对比：**

| 特性 | Supabase | Railway PostgreSQL |
|------|----------|-------------------|
| 免费存储 | 500 MB | 取决于 $5 额度 |
| 地理位置 | 新加坡（亚洲快） | 美国（国内慢） |
| 需要信用卡 | ❌ 不需要 | ✅ 需要 |
| 独立服务 | ✅ 独立 | ⚠️ 占用 Railway 额度 |
| 推荐指数 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ |

---

## 📋 准备工作

### 1. 注册 Supabase 账号

访问 [Supabase](https://supabase.com/) 并注册账号。

**注册方式：**
- GitHub 登录（推荐）
- Google 登录
- 邮箱注册

> 💡 提示：使用 GitHub 登录更方便！

### 2. 创建项目

1. 登录 Supabase Dashboard
2. 点击 **New Project**
3. 填写项目信息：
   - **Name**: `grok2api`（或自定义名称）
   - **Database Password**: 设置一个强密码（记住它！）
   - **Region**: 选择 **Southeast Asia (Singapore)** 或 **Northeast Asia (Tokyo)**
   - **Pricing Plan**: **Free**
4. 点击 **Create new project**
5. 等待创建完成（约 2-3 分钟）

---

## 🔌 获取数据库连接信息

### 步骤 1：进入项目设置

1. 在 Supabase Dashboard 选择你的项目
2. 点击左侧菜单的 **Settings** (⚙️)
3. 选择 **Database**

### 步骤 2：获取连接串

在 **Connection string** 部分，你会看到多种连接方式：

**URI 格式（推荐）：**
```
postgresql://postgres:[YOUR-PASSWORD]@db.xxx.supabase.co:5432/postgres
```

**连接参数格式：**
- **Host**: `db.xxx.supabase.co`
- **Port**: `5432`
- **Database**: `postgres`
- **User**: `postgres`
- **Password**: `[你设置的密码]`

### 步骤 3：转换为 asyncpg 格式

Grok2API 使用 `asyncpg` 驱动，需要将连接串转换为：

```
postgresql+asyncpg://postgres:[YOUR-PASSWORD]@db.xxx.supabase.co:5432/postgres
```

> ⚠️ 注意：将 `postgresql://` 改为 `postgresql+asyncpg://`

---

## ⚙️ 配置 Railway 使用 Supabase

### 方式 1：直接配置环境变量

1. 在 Railway 项目页面点击你的服务
2. 进入 **Variables** 标签
3. 添加或修改以下变量：

```bash
SERVER_STORAGE_TYPE=pgsql
SERVER_STORAGE_URL=postgresql+asyncpg://postgres:YOUR_PASSWORD@db.xxx.supabase.co:5432/postgres
```

> 💡 提示：将 `YOUR_PASSWORD` 和 `db.xxx.supabase.co` 替换为你的实际值

### 方式 2：使用 Railway 的 Secret 变量

为了安全，可以将密码单独存储：

1. 添加 Secret 变量：
```bash
SUPABASE_PASSWORD=your_password
```

2. 在连接串中引用：
```bash
SERVER_STORAGE_URL=postgresql+asyncpg://postgres:${{SUPABASE_PASSWORD}}@db.xxx.supabase.co:5432/postgres
```

---

## ⚙️ 配置 Render 使用 Supabase

### 步骤 1：添加环境变量

1. 在 Render Dashboard 进入你的 Web Service
2. 点击 **Environment** 标签
3. 点击 **Add Environment Variable**
4. 添加以下变量：

```bash
SERVER_STORAGE_TYPE=pgsql
SERVER_STORAGE_URL=postgresql+asyncpg://postgres:YOUR_PASSWORD@db.xxx.supabase.co:5432/postgres
```

### 步骤 2：重新部署

1. 点击 **Manual Deploy** → **Deploy latest commit**
2. 等待部署完成
3. 查看日志确认数据库连接成功

---

## ✅ 验证数据库连接

### 方式 1：查看日志

部署完成后，查看应用日志，应该看到：

```
StorageFactory: 初始化存储后端: pgsql
SQLStorage: 连接成功
```

### 方式 2：访问管理面板

1. 访问管理面板：`https://your-domain/admin`
2. 添加一个 Token
3. 刷新页面，确认 Token 还在（说明数据已持久化）

### 方式 3：在 Supabase 查看数据

1. 在 Supabase Dashboard 点击 **Table Editor**
2. 应该看到自动创建的表：
   - `config`
   - `tokens`
   - `image_metadata`
   - `prompts`

---

## 🗄️ 数据库表结构

Grok2API 会自动创建以下表：

### 1. config 表

存储系统配置：

| 字段 | 类型 | 说明 |
|------|------|------|
| `section` | TEXT | 配置分组 |
| `key` | TEXT | 配置键 |
| `value` | TEXT | 配置值 |
| `updated_at` | BIGINT | 更新时间 |

### 2. tokens 表

存储 Grok Token：

| 字段 | 类型 | 说明 |
|------|------|------|
| `token` | TEXT | Token 值 |
| `pool_name` | TEXT | Token 池名称 |
| `data` | TEXT | Token 详细信息（JSON） |
| `updated_at` | BIGINT | 更新时间 |

### 3. image_metadata 表

存储图片元数据：

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | TEXT | 元数据 ID |
| `data` | TEXT | 图片元数据（JSON） |
| `created_at` | BIGINT | 创建时间 |
| `updated_at` | BIGINT | 更新时间 |

### 4. prompts 表

存储提示词：

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | TEXT | 提示词 ID |
| `data` | TEXT | 提示词数据（JSON） |
| `created_at` | BIGINT | 创建时间 |
| `updated_at` | BIGINT | 更新时间 |

---

## 🔧 高级配置

### 连接池配置

如果需要调整连接池大小，可以在连接串中添加参数：

```
postgresql+asyncpg://postgres:password@db.xxx.supabase.co:5432/postgres?min_size=1&max_size=10
```

**参数说明：**
- `min_size`: 最小连接数（默认 1）
- `max_size`: 最大连接数（默认 10）

### SSL 连接

Supabase 默认启用 SSL，如果需要禁用（不推荐）：

```
postgresql+asyncpg://postgres:password@db.xxx.supabase.co:5432/postgres?ssl=disable
```

### 连接超时

设置连接超时时间（秒）：

```
postgresql+asyncpg://postgres:password@db.xxx.supabase.co:5432/postgres?timeout=10
```

---

## 📊 监控和管理

### 在 Supabase Dashboard 查看数据

1. 登录 Supabase Dashboard
2. 选择你的项目
3. 点击 **Table Editor**
4. 选择要查看的表
5. 可以直接编辑、删除数据

### 查看数据库使用情况

1. 点击 **Settings** → **Database**
2. 查看 **Database size** 和 **Connection count**
3. 免费版限制：
   - 存储：500 MB
   - 连接数：无限制
   - API 请求：无限制

### 备份数据

Supabase 每日自动备份，也可以手动备份：

1. 点击 **Database** → **Backups**
2. 点击 **Create backup**
3. 等待备份完成
4. 可以下载备份文件

---

## 🐛 常见问题

### Q1: 连接失败怎么办？

**A:** 检查以下几点：
1. 连接串格式是否正确（`postgresql+asyncpg://`）
2. 密码是否正确（注意特殊字符需要 URL 编码）
3. 主机地址是否正确（`db.xxx.supabase.co`）
4. 防火墙是否允许连接

### Q2: 密码包含特殊字符怎么办？

**A:** 需要进行 URL 编码：
- `@` → `%40`
- `#` → `%23`
- `$` → `%24`
- `%` → `%25`
- `&` → `%26`

或者在 Supabase 重置密码，使用不包含特殊字符的密码。

### Q3: 如何重置数据库密码？

**A:**
1. 在 Supabase Dashboard 进入 **Settings** → **Database**
2. 找到 **Database password** 部分
3. 点击 **Reset database password**
4. 设置新密码
5. 更新 Railway/Render 的环境变量

### Q4: 数据库满了怎么办？

**A:**
- 免费版限制 500 MB
- 可以删除不需要的数据
- 或者升级到付费版（$25/月，8 GB）

### Q5: 如何迁移数据？

**A:**
1. 从旧数据库导出数据（使用 `pg_dump`）
2. 在 Supabase 导入数据（使用 `psql`）
3. 或者使用 Supabase 的 SQL Editor 执行 SQL

### Q6: Supabase 和 Railway PostgreSQL 可以同时使用吗？

**A:**
- 不建议同时使用
- 选择其中一个作为主数据库
- 如果要切换，需要迁移数据

### Q7: 国内访问 Supabase 快吗？

**A:**
- 新加坡节点：国内访问较快
- 东京节点：国内访问更快
- 比 Railway（美国）快很多

### Q8: 免费版够用吗？

**A:**
- 500 MB 存储足够个人使用
- Token 和配置数据很小（<1 MB）
- 图片元数据也很小（<10 MB）
- 除非生成大量图片，否则完全够用

---

## 💡 最佳实践

### 1. 定期备份

虽然 Supabase 有自动备份，但建议定期手动备份重要数据。

### 2. 监控存储使用

定期查看数据库大小，避免超出限制。

### 3. 清理旧数据

定期清理不需要的图片元数据和日志。

### 4. 使用连接池

合理配置连接池大小，避免连接数过多。

### 5. 安全性

- 不要在代码中硬编码密码
- 使用环境变量存储敏感信息
- 定期更换数据库密码

---

## 🔄 从本地存储迁移到 Supabase

### 步骤 1：导出本地数据

如果你之前使用本地存储，需要先导出数据：

1. 访问管理面板
2. 进入 **Token 管理** → **导出 Token**
3. 保存 Token 列表

### 步骤 2：配置 Supabase

按照上面的步骤配置 Supabase 连接。

### 步骤 3：导入数据

1. 重新部署应用（使用 Supabase）
2. 访问管理面板
3. 进入 **Token 管理** → **导入 Token**
4. 上传之前导出的 Token 列表

### 步骤 4：验证数据

1. 刷新页面，确认 Token 正常显示
2. 在 Supabase Dashboard 查看数据
3. 测试 API 接口

---

## 📊 费用对比

### Supabase 免费版

- **存储**：500 MB
- **API 请求**：无限制
- **连接数**：无限制
- **备份**：每日自动备份
- **费用**：$0/月

### Supabase 付费版

- **Pro ($25/月)**：
  - 存储：8 GB
  - 更多功能
  - 优先支持

### Railway PostgreSQL

- **免费版**：
  - 占用 $5/月 额度
  - 存储取决于额度
  - 可能不够用

### 推荐方案

**个人使用：**
```
Railway Web Service + Supabase PostgreSQL
```

**优点：**
- ✅ Railway $5 额度全部用于 Web Service
- ✅ Supabase 500 MB 免费存储
- ✅ 两者都不需要额外费用
- ✅ 总成本：$0/月

---

## 🎯 总结

### 为什么推荐 Supabase？

1. **完全免费**：500 MB 存储足够使用
2. **访问速度快**：新加坡/东京节点
3. **易于使用**：Web 界面管理
4. **独立服务**：不占用 Railway 额度
5. **高可用性**：99.9% 可用性保证

### 配置步骤总结

1. 注册 Supabase 账号
2. 创建项目（选择亚洲节点）
3. 获取数据库连接串
4. 在 Railway/Render 配置环境变量
5. 重新部署应用
6. 验证数据库连接

### 环境变量配置

```bash
SERVER_STORAGE_TYPE=pgsql
SERVER_STORAGE_URL=postgresql+asyncpg://postgres:YOUR_PASSWORD@db.xxx.supabase.co:5432/postgres
```

---

## 📚 相关文档

- **Supabase 官方文档**：[supabase.com/docs](https://supabase.com/docs)
- **Railway 部署指南**：[DEPLOY_RAILWAY.md](./DEPLOY_RAILWAY.md)
- **Render 部署指南**：[DEPLOY_RENDER.md](./DEPLOY_RENDER.md)
- **部署方案快速指南**：[DEPLOY_GUIDE.md](./DEPLOY_GUIDE.md)

---

**使用 Supabase，让你的部署更简单、更经济！** 🚀
